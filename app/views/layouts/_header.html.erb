    <nav class="navbar-wrapper navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
<%  if media_query_available %>
          <button type="button" class="navbar-toggle" data-toggle="collapse"
                  data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
<%  end %>
          <%= link_to t( "general.application"), root_path, id: "logo" %>
        </div>
<%  if media_query_available %>
        <div class="collapse navbar-collapse">
<%    if logged_in? %>
          <ul class="nav navbar-nav">
            <li>
              <%= link_to current_user.court.name, current_user.court.link %>
            </li>
          </ul>
          <ul class="nav navbar-nav pull-right">
            <li><%= link_to t( "general.log_out"), log_out_path,
                            :method => "delete" %></li>
            <li><%= link_to t( "general.help.short"), help_path %></li>
<%      if enabled? %>
            <li>
              <%= link_to t( "court_days.index.title.weeks"),
                            court_days_path
              %>
            </li>
<%        if admin? %>
            <li><%= link_to t( "users.index.title"), users_path %></li>
<%        else %>
            <li>
              <%= link_to( current_user.name, user_path( current_user)) %>
            </li>
<%        end
        end %>
          </ul>
<%    else %>
          <ul class="nav navbar-nav pull-right">
            <li><%= link_to t( "general.log_in"), log_in_path %></li>
            <li><%= link_to t( "general.help.short"), help_path %></li>
          </ul>
<%    end %>
        </div>
<%  elsif logged_in? %>
        <%= link_to current_user.court.name, current_user.court.link %>
        &nbsp; <%= link_to t( "general.log_out"), log_out_path,
                                           :method => "delete" %>
        &nbsp; <%= link_to t( "general.help.short"), help_path %>
<%    if enabled? %>
        &nbsp;
        <%= link_to t( "court_days.index.title.weeks"), court_days_path %>
        &nbsp; <%= link_to t( "users.index.title"), users_path %>
<%    end
    else %>
        <%= link_to t( "general.log_in"), log_in_path %>
        &nbsp; <%= link_to t( "general.help.short"), help_path %>
<%  end %>
      </div>
    </nav>

